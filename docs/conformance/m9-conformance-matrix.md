# M9 Conformance Matrix

> **Category:** Target architecture
>
> **Status:** M9 target; implementation blocked pending accepted delivery
>
> **Milestone:** M9
>
> **Reviewed baseline:** `68808fb1dd5bfbd644a74fcfe916870428bcdd59`
>
> This matrix becomes the permanent M9 observable/proof contract only after the
> M9A0 package containing it is owner-accepted, squash-merged, and accepted-main
> validated. Every M9 row is initially `blocked`; target acceptance does not claim
> production implementation.

[ADR 0010](../adr/0010-visual-composition-and-animation.md) owns M9 architecture.
[ADR 0011](../adr/0011-m9-path-fill-shadow-spread-clarification.md) narrowly amends
ADR 0010 only for path fill/stroke closure, ellipse degenerate/boundary behavior,
and ordinary shadow spread/effect-bound semantics.
[ADR 0012](../adr/0012-m9-group-ordering-clarification.md) narrowly amends ADR 0010
only for composition-group ordering, grouping scope, node-effect isolation/group
lifetime, and the exact relationship to inherited M6 `PAINT-05`. M3 owns mounted
identity/lifecycle/invalidation; M4 owns logical time, scheduling, wake/redraw, and
trace; M5 owns semantic identity/publication/testing; M6 owns the renderer-neutral
paint/hit publication model and `ResourceRef`; M7 owns concrete renderer/host/resource
edges; M8 owns production style/layout/text and current property-effect
classification. This matrix adds only new M9 observations.

```text
25 total unique rows
0 owner-accepted
0 implementation-complete
0 proof-complete
25 blocked
0 duplicate IDs
0 invalid statuses
0 invalid schemas
```

All rows are `Required`. Serial M9 delivery is M9A visual/composition production,
M9B deterministic transitions/timelines, then M9C integrated closure.

A dependency-owned public path/paint vocabulary, retained second scene/tree,
renderer-owned animation clock/timeline, per-frame action queue, stale hit/semantic
geometry under visual motion, ambient preference policy, custom backend shader escape
hatch, software expected renderer, cache/revision counter acting as behavior authority,
or compatibility shim preserving replaced pre-1.0 visual authority cannot satisfy M9.

## M9A — production visual/composition vocabulary and renderer realization

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| M9VIS-01 | Public visual/composition vocabulary is RunenUI-owned and responsibility-normalized: common node background-brush/outline/shadow/opacity/presentation policy resolves through computed style/runtime; arbitrary shape/path/image content remains owner-local paint contribution; composed transforms/groups/effect bounds remain immutable runtime publication. No Lyon, Kurbo, Peniko, Vello, wgpu, WGSL, native, mounted, or dependency scene type becomes public authority. | Public-API/style/paint/publication ownership corpus | Duplicate-authority, forbidden-type/public-dependency, item-transform-as-node-presentation, and second-scene audit | Visual schema/provenance/ownership report | M9A | blocked | Required |
| M9VIS-02 | Validated rect/rounded-rect/ellipse/path geometry has finite logical coordinates, explicit contour/fill-rule semantics, deterministic logical bounds, and fill-hit behavior independent of renderer tessellation. Each `move` starts a contour; move-only contours have no coverage; every segment-bearing contour is logically closed for fill by a synthetic final-to-first edge when no authored `close` exists; authored/synthetic fill boundaries are inside coverage; non-boundary points use the authored non-zero/even-odd rule. Ellipse zero width/height is empty and positive-extent analytic ellipse boundary is inside without revising inherited M6 half-open rect/rounded-rect semantics. Structural path equality/content is RunenUI-owned; logical containment remains core-owned, and dependency flattening tolerance/degenerate defaults cannot become public semantics. Malformed contour verb ordering rejects and paint never implicitly manufactures a physical hit target. | Shape/path/open-fill/boundary/ellipse/equality/bounds/hit/core-adapter corpus | Non-finite/malformed/move-only path misuse, missing open-fill closure, dependency/raster/tessellation/tolerance-derived containment, allocation/dependency identity, ellipse-degenerate drift, and paint-derived-hit audit | Path contour/validation/bounds/hit/boundary/adapter records | M9A | blocked | Required |
| M9VIS-03 | Stroke semantics use one centered-on-boundary placement with finite non-negative width; butt/round/square caps; miter/bevel/round joins; finite miter-limit ratio >= 1 with deterministic bevel fallback; zero width has no coverage and is never a backend hairline. An authored `close` adds the real final-to-first centerline edge and seam join; an open contour retains two capped endpoints and its synthetic fill-closing edge is never stroked. A second `close` or segment after close without a new `move` rejects. Logical bounds include complete cap/join/closing-edge coverage; inside/outside stroke is not a hidden backend option. | Stroke geometry/open-vs-closed/seam/cap/placement/bounds corpus | Synthetic-fill-edge-as-stroke, backend-default/hairline/inside-outside/invalid-miter, invalid post-close continuation, and truncated-bound corpus | Stroke contour/closure/normalization/placement/bounds records | M9A | blocked | Required |
| M9VIS-04 | Solid, linear-gradient, and concentric radial-gradient brushes preserve primitive-local logical geometry, nondegenerate linear endpoints / positive radial radius, stable nondecreasing stops with deterministic hard-stop side semantics and endpoint extension, straight-alpha sRGB8 inputs, and premultiplied linear-sRGB interpolation without backend-specific color policy; the M8 color-only background is cleanly replaced by brush-valued background rather than retained as parallel authority. | Brush/gradient/color/style-cutover corpus | Degenerate-gradient, unsorted/non-finite stop, hidden color-space, backend-brush, and duplicate-background audit | Gradient normalization/interpolation/style provenance records | M9A | blocked | Required |
| M9VIS-05 | Image ordinary-fit geometry uses exact non-zero neutral intrinsic dimensions supplied by the same logical resource owner as the complete opaque `ResourceRef`, plus a positive normalized source crop, finite normalized alignment, and logical destination: `fill` independently scales axes, `contain` uses min uniform scale, `cover` max uniform scale, `none` scale 1, and `scale-down` chooses none when fitting else contain. The M7 provider payload extent for that same ref must exactly equal the descriptor intrinsic extent; mismatch is a structured resource/realization failure rather than renderer choice, hidden rescale, or ref remint. Runtime/publication resolves exact source/destination geometry; fit/crop/layout/raster/device state never remints `ResourceRef`. | Image descriptor/provider-correlation/fit/crop/alignment/resource-identity corpus | Invalid-crop/alignment, missing metadata, descriptor/provider extent mismatch fallback, renderer-fit, hidden rescale, fit-key/resource-remint, and scale/device-bound identity audit | Image geometry/resource/provider correlation and mismatch records | M9A | blocked | Required |
| M9VIS-06 | Nine-slice is a distinct initial image mode with exact source rect/insets, logical destination edge widths, proportional normalization when opposing edges exceed destination extent, non-negative center, stretched corners/edges/center under the accepted axis rules, no implicit tiling, and the same complete image `ResourceRef`. | Nine-slice geometry/mapping corpus | Fit/nine-slice ambiguity, tiling fallback, negative-center, overlapping-source, and reminted-resource corpus | Slice normalization/placement records | M9A | blocked | Required |
| M9VIS-07 | Immutable snapshot-local composition groups preserve nesting and exact ordered membership while qualifying inherited M6 `PAINT-05` only where atomic isolation makes outside interleaving impossible. Runtime first derives the exact stable global M6 pre-group order `(layer, mounted logical preorder, contribution-local order)`. Every item has at most one immediate group and group parentage is single-parent/acyclic; sibling group descendants may interleave in pre-group order. Each non-empty group anchors at its earliest descendant pre-group item and recursively contracts to one atomic parent entry there; with no groups M6 order is unchanged, and empty groups have no coverage/effects and may be omitted. Descendant `SceneLayer` values remain pre-group ordering facts inside an isolated group but cannot escape that ancestor group and re-enter parent ordering after contraction. Primitive geometry uses item-local transform -> node presentation transform -> final owner placement; contribution clips retain independent clip-local-to-owner transforms, then receive node presentation + owner placement. Each clip list remains conjunctive exactly as M6 requires; structural group scopes accumulate outer-to-inner. Item clips constrain items; group children source-over compose; group effects derive from composed coverage; group clips constrain the complete group result/effects; group opacity applies once before source-over into the parent at the frozen first-member anchor. Groups acquire no mounted/lifecycle/semantic/cross-publication authority. | M6 pre-group order/transform/clip-intersection/nested-group/first-member-contraction/sibling-interleaving/cross-layer-non-escape/empty-group composition corpus | Renderer/group-layer resort, descendant-layer escape, transform-order swap, item-transform leakage into clips, clip-list-order-dependent coverage, per-child-opacity substitution, malformed group parentage, backend blend-policy, and retained-second-tree audit | Pre-group order/transform/clip/group structure/anchor/contraction records | M9A | blocked | Required |
| M9VIS-08 | Ordinary node opacity and drop shadows use runtime-derived composition groups over the node's composed mounted visual subtree: descendants do not inherit the style property but their paint contributes to ancestor group coverage, and independently affected descendant nodes form nested groups. Outside active motion, the node-effect group exists exactly when effective node opacity is not `1` or the ordered ordinary-shadow list is non-empty; default opacity `1` plus an empty list does not isolate every node, while a non-empty shadow list remains an ordering fact even if its current visual coverage is empty/transparent and may not be optimized away. Group opacity applies exactly once; node outline remains ordinary node paint and does not itself create isolation. Ordinary drop shadows preserve exact neutral offset, non-negative sigma, finite signed spread, color, and the inherited ADR 0010/0012 pre-shadow composed child-coverage authority. Signed spread is rotation-invariant Euclidean morphology over that exact composed logical coverage: positive spread is dilation by a closed disk of radius `s`, negative spread is the corresponding disk erosion, zero spread preserves coverage, and complete erosion is empty. Offset follows spread; blur support is truncated to no farther than `3 * sigma` on each logical axis. Framework effect/damage bounds remain deterministic conservative logical bounds derived from neutral scene facts: positive spread expands conservative pre-shadow coverage bounds by `s` per side, zero/negative spread may conservatively retain those bounds when tighter erosion bounds are unavailable, then offset and `3 * sigma` expansion apply. Renderer raster/kernel/cache choices cannot redefine subtree scope, inherited composed coverage, spread, group existence, grouping, or framework bounds. Opacity/shadows expand or alter paint/damage only and do not silently alter layout/hit/focus/semantic bounds or physical hit ordering. | Node-subtree/static-group-existence/identity-default/visually-empty-shadow/nested-effect-group/group-opacity-once/shadow/inherited-coverage/spread-rotation/erosion/effect-bounds/support corpus | Property inheritance as effect scope, own-item-only node effect, identity-groups-every-node, transparent-shadow group elision, per-child opacity, second shadow-coverage/source-alpha/image-sampling authority, axis-aligned-square/raster-kernel/backend-defined spread, orientation-dependent spread, infinite support, non-empty visual result after complete erosion, undersized logical bounds, invalid spread, and semantic/layout/hit inflation audit | Node-group/subtree/group-existence/inherited-coverage/spread-operation/effect-bound/support/realization diagnostics | M9A | blocked | Required |
| M9VIS-09 | The extension boundary remains explicit non-exhaustive neutral primitives/effects/resources; arbitrary shader/filter/backend callbacks, `Any` render nodes, device handles, or dependency-owned scene subtrees are not public escape hatches. | Extension/capability corpus | Shader/WGSL/wgpu-handle/custom-scene authority audit | Unsupported-capability diagnostics | M9A | blocked | Required |
| M9VIS-10 | Lyon adoption uses only exact required private subordinate subcrates/patches after proof of RunenUI Rust 1.93 compatibility, features/transitives/licenses, build impact, and relevant shared corpus/benchmark. `runenui_core` may privately adapt `lyon_path`/`lyon_algorithms` behind RunenUI structural path values for logical geometry while dependency tolerance/identity remains non-authoritative; `runenui_render_wgpu` may use `lyon_tessellation` only for disposable fill/stroke realization and owns GPU/group/shadow caches reconstructible after cache/device loss. The Lyon meta crate is not adopted by default, and Kurbo/Peniko/Vello do not become parallel geometry/paint/renderer authorities merely for convenience. | Exact-dependency/MSRV/license/features/build + core-adapter + real-wgpu realization/re-realization corpus | Public-Lyon-type, Lyon-as-path-identity, renderer-owned logical containment, dependency-default semantics, unproved-patch, retained-Lyon-tree, second-geometry-model, Peniko scene, Vello/second-wgpu-renderer, and cache-as-authority audit | Dependency/adopt-build/core-adapter/renderer-cache records | M9A | blocked | Required |

## M9B — deterministic transitions and timelines

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| M9MOTION-01 | All animation sampling uses the accepted runtime `MonotonicClock`; one staged surface candidate observes one exact monotonic instant with no wall-clock read, renderer timestamp, sleep, background animation thread, or second clock. A new motion start instant is committed from that candidate time only when staging succeeds. | Manual-clock/same-sample/start-commit corpus | Wall-clock/renderer-time/second-clock and failed-stage-time-consumption audit | Sample/start instant records | M9B | blocked | Required |
| M9MOTION-02 | Style transitions start only from canonical resolved target changes, including hover/focus/active/disabled state, and interruption/reversal starts from the currently sampled presented value rather than stale prior target state. | Interaction/style-transition corpus | Second interaction-state, target-jump, and stale-start-value corpus | Target/start/sample provenance | M9B | blocked | Required |
| M9MOTION-03 | Explicit timelines are declarative authored values with stable animation identity, at least two strictly increasing normalized keyframes whose endpoints are exactly 0/1, one easing per segment, checked duration/delay, and repeat of one/non-zero finite total iterations/forever; delay applies once and initial M9 has no implicit autoreverse. Exact mounted-generation lifecycle preserves unchanged spec and deterministically cancels/replaces/removes/unmounts/shuts down. For one property, an active explicit timeline is the sole sampled owner and timeline termination resumes from its current/final sample toward the then-current style target under normal transition policy. | Keyframe/spec/reconciliation/timeline-lifetime/same-property-precedence corpus | Duplicate/missing endpoint keyframe, hidden autoreverse, widget-local timer/state-machine, stale-generation, additive same-property animation, and competing transition audit | Spec/start/replace/cancel/lifetime/precedence records | M9B | blocked | Required |
| M9MOTION-04 | Linear/cubic-Bezier easing and timeline normalization have exact endpoints, deterministic delay/iteration/zero-duration semantics, finite checked time arithmetic, and forever timelines never claim natural completion. Ordinary samples are pure functions of retained endpoints/keyframes plus exact normalized progress; they never incrementally integrate previous frame samples. Non-final iteration boundaries restart at keyframe 0; final boundaries sample keyframe 1 and complete. Zero-duration/zero-delay terminates in the same successful start candidate; zero-duration with delay holds start until its deadline. | Easing/timing/delay/iteration/zero-duration/history-independence corpus | NaN/out-of-range/overflow/phantom-frame, ambiguous boundary/completion, frame-rate integration, and accumulated-rounding corpus | Normalized progress/endpoint/sample/completion records | M9B | blocked | Required |
| M9MOTION-05 | Motion targets are explicit typed derived overrides, never mutation of authored/application/widget state. Compatible color/brush/opacity/presentation-transform/radius/shadow/padding and accepted same-domain numeric layout values interpolate continuously; resource/path/incompatible-gradient/auto-intrinsic/typography identity changes follow explicit discrete rules. Sampled effective layout values feed the accepted runtime/Taffy/text authority, and arbitrary paint-item indices are not stable animation targets. | Property-target/compatibility/interpolation/effective-layout corpus | Generic-Lerp, authored-state mutation, animation-specific layout engine, path-morph/resource-crossfade/text-scale, and paint-index target audit | Motion-target/interpolation/effective-value records | M9B | blocked | Required |
| M9MOTION-06 | Sampled property changes use exact direct-effect classification, transitive invalidation, and stage-local cache compatibility. Paint-only motion avoids layout/text; presentation motion updates paint/hit/focus/semantic geometry without relayout; layout motion recomputes layout/dependents; text-metric changes use the accepted text/layout authority. Node opacity/shadow motion recomposes paint/group structure under ADR 0012 without relayout: a live unsuppressed style transition keeps the node-effect group when either its current sampled effective value or terminal target requires isolation; a live unsuppressed explicit timeline keeps it when either its current sample or any accepted keyframe value in that live timeline specification requires isolation; a terminal post-completion identity state may remove it in the same staged candidate. Time advance alone does not invalidate stages whose exact effective inputs are unchanged or force invalidate-all. | Differential animated-invalidation/cache-reuse + transition/timeline group-lifetime corpus | Under-invalidation, motion-group renderer optimization, identity-keyframe/repeat group churn, suppressed-motion hidden grouping, identity-target premature group removal, time-invalidates-all, frame-counter cache key, renderer-text-scale, stale-product, and unconditional invalidate-all audit | Property-effect/dirty-phase/group-existence/cache-compatibility records | M9B | blocked | Required |
| M9MOTION-07 | Mandatory M8 preference overrides remain above motion. Reduced-motion behavior derives only from explicit `StylePreferences::reduced_motion` plus bounded RunenUI policy: transitions/finite decorative timelines default to snap-to-end; repeating decorative timelines default to hold-initial, suppress their live timeline and continuous redraw, and restart from the preference-change instant rather than accruing hidden elapsed time when reduced motion is later disabled; preserved essential motion requires explicit declaration. Conflicting explicit motion cannot animate around a currently mandatory high-contrast/property override. A transition/timeline suppressed by mandatory preference policy also contributes no ADR 0012 motion-specific group requirement: group existence derives from the governed effective property fact and any surviving effective motion source, so preference suppression cannot be bypassed by an otherwise invisible stacking context. | Preference-precedence/motion-policy/suppression-restart + suppressed-group-authority corpus | Ambient platform/renderer policy, motion-above-preference, suppressed-motion hidden stacking context, hidden speed multiplier/time accrual, redraw-while-held, and ignored-preference audit | Preference/motion decision/suppression/group-existence/restart/cancellation records | M9B | blocked | Required |
| M9MOTION-08 | Active visible motion uses existing redraw/wake authority; redraw acknowledgement may request another frame while motion remains active, but runtime does not enqueue one canonical FIFO action per sample or invent a fixed framework frame rate. Equal consecutive published samples may still request a future redraw while motion remains active without minting scene/resource identity. | Redraw/vsync/manual-time/equal-sample corpus | Per-frame FIFO, busy settle-loop, hidden 60-Hz timer, lost-redraw, and redraw-as-resource-identity audit | Redraw/wake/active-motion records | M9B | blocked | Required |
| M9MOTION-09 | Live motion compatibility is exact mounted generation + target/source identity + exact accepted specification/endpoints/start instant/preference facts. Replacement samples old motion at the same candidate instant, orders cancel/replace before replacement start, and uses that sample as continuity start where applicable. Ordinary sampling does not mint a motion identity. Inherited product/publication revisions change only under accepted tuple/product rules, never time alone. Motion state/sample/terminal completion and ADR 0012 node-effect group existence commit atomically with staged products; replacement group existence uses the surviving effective replacement transition terminal target or explicit timeline specification after cancellation/replacement and preference-precedence decisions, renderer failure never advances either motion or group state, and retained retry uses the exact already-sampled publication. | Motion-identity/revision/replacement-order/publication-failure/retry + transition/timeline group-existence atomicity corpus | Global frame/sample revision authority, revision-on-time-only, partial generation, renderer-driven group lifetime/completion, preference-suppression/group-state drift, retry-resample, and replacement/group-order ambiguity audit | Motion compatibility/product revision/cancel-start/group-existence/candidate/commit/retry correlation | M9B | blocked | Required |
| M9MOTION-10 | Animation inspection extends the canonical runtime diagnostics/trace with target/start/replacement/suppression/restart/cancellation/completion/sample/preference/cache-decision facts without creating a second mutable animation/cache log or requiring application actions to implement debug/tween traits. Initial completion emits no application action. | Trace/inspection/order corpus | Parallel event/cache ledger, completion-action side channel, debug-trait, and action-channel audit | Canonical motion trace records | M9B | blocked | Required |

## M9C — integrated production closure

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| M9INTEG-01 | One runtime-sampled node presentation transform uses the frozen scale -> rotate -> presentation-translate order about normalized origin, followed by layout owner placement, and is composed consistently into visible paint, physical hit testing, directional-focus geometry, semantic bounds, and presentation-relative clips. Item-local transforms remain paint-local; clip-local transforms remain independently owner-local. Hit uses exact transformed shape, focus/semantics use deterministic axis-aligned bounds of transformed authoritative geometry, and singular transforms have inherited empty-hit behavior rather than stale layout fallback. | Transform-order/paint-hit-focus-semantic/clip correlation corpus | Component-order drift, item-transform-to-clip leakage, paint-only presentation transform, stale hit/accessibility, and untransformed fallback audit | Node/sample/transform/geometry correlation records | M9C | blocked | Required |
| M9INTEG-02 | Deterministic public/headless proof advances `ManualClock` explicitly and observes exact intermediate/end/delay/repeat/replacement/preference/cache/layout/text/semantic results through ordinary public runtime contracts without sleeps, private expected runtime, alternate scene engine, or software expected renderer. | Public manual-time integration corpus | Sleep/wall-clock/private-model/alternate-engine audit | Fixture/time/cache/publication diagnostics | M9C | blocked | Required |
| M9INTEG-03 | Real-wgpu proof renders the same accepted neutral paths/strokes/gradients/images/nine-slice/groups/shadows and representative transition samples, including retained-publication retry and disposable cache/device re-realization, without renderer-owned style/timeline/hit/semantic authority or resource rebinding by raster/device state. | Real-wgpu visual/motion/contact-sheet/re-realization corpus | Vello/second-renderer, software expected renderer, renderer timeline, resource-rebinding, and retained-cache-authority audit | Runtime/scene/resource/renderer correlation records | M9C | blocked | Required |
| M9INTEG-04 | Representative canonical hover/focus/active transitions visibly and deterministically exercise the M8 interaction/style path plus M9 transition path; no application-maintained hover state, widget timer, or showcase-only animation authority is required. | Interactive-state transition corpus | Duplicate hover state/showcase timer/direct renderer animation audit | Interaction-style-motion provenance | M9C | blocked | Required |
| M9INTEG-05 | Final M9 authority cleanup removes/revises bounded M6/M8 assumptions superseded by accepted M9 production visuals/motion: color-only background, specialized public `FillRect`/`StrokeRect` primitive authority, the old exact-mapped image variant kept as a parallel path, duplicate shape APIs retained only for `Copy`, and any competing composition/motion path are removed/reconciled; `ShapedTextRun` remains the sole production text-paint authority. Current docs/API expose one truthful path with no compatibility alias, hidden renderer fallback, or obsolete proof authority preserved for convenience. | Source/API/current-truth cleanup corpus | Duplicate background/rectangle/image/shape/text/composition/motion/compatibility-authority audit | Repository authority/deprecation audit | M9C | blocked | Required |

## Closure rule

M9 is conformance-complete only when all 25 rows are `owner-accepted` on accepted
default branch. Each serial implementation slice must be separately owner-accepted,
squash-merged, accepted-main validated, and followed by bounded current-truth/
conformance reconciliation before its rows become `owner-accepted`. M9 closes only
after final integrated reconciliation and accepted-main validation. Later M10/M11
work may consume the accepted visual/motion contracts but must not move their clock,
scene, property, hit, semantic, cache, resource, or renderer authority elsewhere.